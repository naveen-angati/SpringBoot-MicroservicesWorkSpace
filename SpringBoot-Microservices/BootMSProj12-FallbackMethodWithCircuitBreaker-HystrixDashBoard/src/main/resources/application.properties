# Rest SErvice/MicroService port number
server.port=9001

#  activate all the end points (actuators)
management.endpoints.web.exposure.include=*

#  enable  hystrix streaming list
hystrix.dashboard.proxyStreamAllowList=*

